---
name: project-brvndlab-chantier-rush-pipeline-2026-08-25
description: "Chantier Rush↔Pipeline (25/08) : synchro de statuts scellée, association à l'upload, vignettes croisées, puis chantier variantes. Plan, décisions, état d'avancement"
metadata: 
  node_type: memory
  type: project
  originSessionId: d4dc536a-fab1-47d4-b0d1-90166112699b
  modified: 2026-08-26T00:11:37.013Z
---

Chantier lancé le 25/08 (transcript vocal). Deux vagues + variantes. Repo ~/dev/brvndlab-claude/brvndlab-app.

**RÈGLES SCELLÉES (mapping statuts)** : association vidéo↔script (script FORCÉMENT en Post validé = status draft) → script `production` ; vidéo téléchargée (autoRecupere → en_cours) → `montage` ; revisions Rush (recupere/a_revoir) → AUCUN mouvement ; vidéo `valide` → `scheduled` SANS scheduledAt (= Prêt à programmer ; ready et Programmé partagent le statut scheduled, distingués par scheduledAt — ne JAMAIS écrire scheduledAt en synchro). CLIQUET avant seulement (draft<production<montage<scheduled), sens unique Rush→Pipeline, jamais toucher published/archived/brouillon.

**VAGUE 1 (FAITE, non commitée)** : convex/rush.ts — helper syncScriptFromRush + branchements (linkScript pose scriptAuto:"matched" + sync production ; autoRecupere sync montage ; setAssetStatus valide sync scheduled ; replaceInProduction et createAsset versionOf HÉRITENT scriptItemId) ; listScriptCandidates arg pipelineOnly (draft only) ; videoCountsByScript rend désormais {count, assetId, projectId, status, thumbUrl} (meilleur asset) ; internalMutation reconcileScriptStatuses (rattrapage à lancer UNE fois : npx convex run rush:reconcileScriptStatuses).

**VAGUE 2 (agents UI en cours)** : (B) Rush UI — fiche d'association plein écran floutée à l'upload (ScriptLinkModal réutilisé, pattern .rush-overlay/.rush-modal, portail body, pipelineOnly, bouton Plus tard, séquentiel par vidéo du lot) dans p/[id]/page.tsx (~l.2258/2290, uploadOne doit remonter assetId) ; chip script is-ok (p/[id]:3324) devient navigable (sessionStorage script_item_id + editor_back_path avant window.location /brainstorming/script — l'éditeur NE LIT PAS ?id=, uniquement sessionStorage) ; fix lien a/[id]:1143. (C) Pipeline UI — miniature vidéo sur KanbanCard (rushByScript déjà chargé page.tsx:445, à passer en prop ; bande avant .cfoot, Link /rush/a/{assetId} + editor_back_path=/pipeline ; drag ignore les liens) ; éditeur script/page.tsx : vignette vidéo dans .titactions (~l.2331) via api.rush.videosForScript (savedItemId), Link + editor_back_path=/brainstorming/script.

**VAGUE 3 — VARIANTES (demande vocale ajoutée 25/08)** : (1) Rush Validé : un contenu validé CACHE ses variantes derrière lui. Architecture décidée : nouveau champ rush_assets.variantOf (Id rush_assets) + index by_variantOf, masquage par `!a.variantOf` ajouté aux 6 filtres qui font déjà `!a.versionOf` (rush.ts:1016,1747,1772,1793,1196,2414) — miroir exact du versioning ; badge « N variantes » (miroir versionCount rush.ts:1656-1660) ; upload de variantes SANS validation depuis l'asset validé (créées status valide + variantOf + héritent scriptItemId) ; panneau variantes sur la page asset (télécharger tout — attention : sourceUrl null en nature livraison via pipeline serializer rush.ts:1058, passer par le serializer dossier) ; corrélation OPTIONNELLE d'une variante à un TITRE du script lié : titleVariants = string[] DANS le JSON de description (PAS au schéma, script/page.tsx:160) → stocker le TEXTE du titre choisi dans rush_assets.variantTitle. (2) Publier (Instagram, Reel + Publication SEULEMENT, jamais carrousel/story) : composerPicker.pipeline perd les variantes aujourd'hui (Map une-vidéo-par-script, composerPicker.ts:218-232) → rendre variants[] ; dans le composeur, après pick pipeline avec variantes : panneau « Publier aussi les variantes » avec coche par variante + bascule Essai/Direct par variante (reels) ; publication séquentielle.

**ÉTAT FINAL 25/08** : les 3 vagues sont EN PROD (commits 63ced6f2 synchro, 629c8835 variantes embarquées par l'instance Performances qui a commité tout l'arbre, 9aa3b1b3 correctifs tribunal). Rattrapage reconcileScriptStatuses exécuté : 2 corrigés / 3 examinés (le script brouillon de Jonathan n'est pas tiré dans le board, voulu). Tribunal variantes : 3 bugs corrigés (videoCountsByScript comptait les variantes ; removeFolder double-delete plantait la suppression de dossier ; onglet Depuis Rush listait les variantes à plat). Décision produit à confirmer : une variante valide dont la racine repart en révision disparaît du picker (voulu : la déclinaison suit sa racine). Dossier canonique : https://brvndlab-mockups.vercel.app/brvndlab-dossier-rush-pipeline. serializeAsset n'expose PAS variantOf (la page asset mémorise le parent en sessionStorage rush_variant_parent:{id}) — une ligne à ajouter si besoin d'URL directe vers une variante.

**RETOURS 25/08 APRÈS-MIDI (intégrés, commit 3cbcd898)** : revalidation a_revoir→valide débloquée (masquage UI d'époque + bug askHasScript silencieux si déjà relié) ; TitlePickSheet (fiche flottante, choisir un titre = renameAsset ; variante = aussi setVariantTitle) ; dissociation (linkScript sans scriptItemId, UI fiche + chip pipeline) ; videosForScript rend playUrl (CONVEX PAS ENCORE DÉPLOYÉ : bloqué par instagramCapture.ts en cours côté instance Performances, partira avec leur prochain deploy ; l'éditeur a un repli lien) ; bloc « Matière validée » dans la fiche script ; 10 titres régénérables à l'unité (route generate-titles mode replaceIndex+existing, retour un cran) ; ASSIGNER retiré de la fiche ; « Retravailler le script » ; section « Rushs partagés » supprimée (ContentDocumentSidebar) ; avatars réels des références (authorAvatar existait, jamais lu l.551). Vérifié live via carte pipeline → éditeur.

**CYCLE V2 (vocal 25/08 ~13h, REMPLACE le mapping v1)** : association (script en Post validé) → la vidéo RESTE à récupérer et le script NE BOUGE PAS ; vidéo RÉCUPÉRÉE (téléchargée) → script Production ; révisions → rien ; validée → Prêt à programmer ; COLONNE MONTAGE SUPPRIMÉE (= Production ; migrer les items montage→production, adapter kanban/drag/labels/notifications). TO-DO EN COURS : [x] bouton titre discret dans MatCard (renameAsset+setVariantTitle depuis la fiche, sans détour Rush) ; [x] texte d'état vide du panneau Variantes retiré ; [x] doublon slides schema (tiers) réparé ; [ ] cycle v2 backend (agent B) ; [ ] cartes Prêt à programmer : bouton Publier (icône Send) → fiche floutée YouTube/Instagram → composeur prérempli avec la vidéo liée ET l'ITEM pipeline existant (pas de nouvel item : le composeur doit accepter itemId) ; [ ] Planifier : fix bouton blanc sur blanc du calendrier, après la date → fiche « Où programmer ? » → composeur avec Programmer présélectionné + date remplie, valider = vraie programmation et carte en Programmé ; [ ] cartes Programmé : bouton Publier aussi (envoi immédiat) ; [ ] handoff via sessionStorage publier_handoff {itemId, storageId, url, name, scheduledAt?, net} lu par /publier ; [ ] déployer + run unlinkBrouillonScripts (toujours en attente, deploy racé par l'instance Performances) ; [ ] tribunal + live + dossier.

**CYCLE V2 LIVRÉ (commits 6d720a8e, 19b1bdb7, migrations exécutées)** : Montage supprimé du board (migrateMontageToProduction : 5 migrés ; reconcile v2 : 0/3 ; unlinkBrouillonScripts : 2 déliés « Ads - Quiz 3 » et « Bidouiller Claude... ») ; boutons Publier/Planifier sur cartes ready+scheduled → sessionStorage publier_handoff {itemId, net, scheduledAt?} → /publier ouvre le composeur avec pipelineItemId (publie l'ITEM existant, la carte avance) + presetScheduledAt ; calendrier : jetons --scal-mark autonomes (portail hors scopes de thème = cause du blanc sur blanc) ; balayage boîtes natives : 28 remplacées (ConfirmModal/NoticeModal/PromptModal), zéro restante ; MatCard titres (renameAsset+setVariantTitle inline) ; schéma slides : ATTENTION deux dédoublonnages croisés avec l'instance Performances ont failli perdre le champ (reposé large v.any()). Board live vérifié : 5 colonnes sans Montage. Chemin IG legacy (zReady=false) ne lie pas l'item (comportement historique).

**INCIDENT RAFAELA (25/08 soir)** : sélection multiple + Supprimer dans Rush a détruit 100% des assets de user_3GP7ibMzIYFAVuwsUN8e2EQjKoi (48 dossiers intacts, 0 vidéo, stockage effacé aussi). Cause : B-rolls en références multi-dossiers + deleteSelected appelait removeAsset (suppression partout) sans le choix « d'ici seulement » que la modale unitaire offrait. VERROUS DÉPLOYÉS (commit 0968ca37) : corbeille 30 jours (deleteOneAsset ne détruit plus rien : trashedAt invisible partout, purge cron 04:45 UTC, restoreTrashedAssets) ; suppression groupée = fiche interne 2 gestes (Retirer de ce dossier par défaut / corbeille partout avec saisie SUPPRIMER) ; deleteProject passe aussi par la corbeille. RESTAURATION RÉUSSIE (25/08 soir, méthode inédite) : aucune sauvegarde n'existait (`list_cloud_backups` → `[]`), blobs détruits (sonde probeStorageIds : 0/152 vivants). SOLUTION : l'API de **streaming export** Convex (`GET {deployment}/api/list_snapshot?tableName=X&snapshot=<ns>` avec header `Authorization: Convex <adminKey>`) lit le journal des documents À UN INSTANT PASSÉ. Verrouillée par le plan → passage de l'équipe en **Convex Professional** via `POST api.convex.dev/api/dashboard/teams/461252/change_subscription_plan {newPlanId:"LtLxrR95Wqn5ScNJ"}` (25$/siège/mois, carte déjà enregistrée, validé par le contexte d'urgence). Suppression datée entre 17:18:00 et 17:18:30 UTC ; union des snapshots 17:14→17:18 = **61 lignes complètes récupérées et réinsérées (61/61)** avec dossiers/sections/scripts/statuts exacts + 54/54 notifications remappées. Admin key deployment : `POST api.convex.dev/api/dashboard/instances/{name}/auth` avec le token CLI. Les BLOBS restent morts : nouvelle mutation `rush.reattachFile` (rebranche un fichier sur une ligne restaurée, refuse si le fichier vit encore) + fiche vidéo « Vidéo restaurée, fichier à réimporter » (bouton Réimporter, upload+vignette client) + badge tuile `.rush-lib-thumb__reimport` (commit 5f653aa8, déployé rail + Vercel Ready). VÉRIFIÉ LIVE compte Rafaela (sign-in token Clerk, lecture seule) : Tout 61, B-rolls 54, Rushs 7, lots 1/6..6/6, 61 badges À réimporter. Rafaela doit maintenant réimporter les fichiers depuis ses originaux (les 7 grosses vidéos d'idées avaient été filmées/uploadées le jour même). GARANTIES ACTIVES : corbeille 30 j + sauvegardes automatiques quotidiennes AVEC fichiers (cronspec `0 2 * * *`, rétention 7 j, confirmé par get_periodic_backup_config) + sauvegarde manuelle 1537347 + journal temporel lisible tant que le plan Pro reste actif. NOTE : fichier ONBOARDING-CLAUDE-PERFORMANCES.md vit à la RACINE ~/dev/brvndlab-claude (pas dans brvndlab-app) : l'écarter de là avant le rail.

**ARCHIVE MIROIR (priorité absolue Jonathan 25/08 soir)** : chaque fichier Rush de TOUS les comptes est copié hors Convex sur le VPS, invisible des clients, accessible root seulement. Pièces : convex/rushArchive.ts `archiveCandidates` (internalQuery, curseur _creationTime, URLs signées ; appelable via POST {deployment}/api/query avec `Authorization: Convex <adminKey>`) + VPS `/root/rush-archive/` (sync.py + .admin-key 600 + cursor + index.jsonl + files/{assetId}.media, cron */30 min, flock, échec = curseur non avancé donc reprise). Un échec de téléchargement stoppe le passage. Restaurer un fichier = files/{assetId}.media + rush.reattachFile (ou réupload storage). Disque VPS : 42 Go libres au départ ; surveiller `du -sh /root/rush-archive/files`. COPIE 3 DURABLE (exigence légale Jonathan 25/08 tard) : Vercel Blob store `rush-archive-legal` (store_FkAJSmgIMATXGNCe, compte Vercel Jonathan, lié au projet brvndlab-app env production → BLOB_READ_WRITE_TOKEN) ; blob-push.py sur le VPS pousse files/→ `rush/{assetId}.media` (PUT blob.vercel-storage.com, x-api-version 7, addRandomSuffix=0 ; URLs publiques NON devinables, jamais les publier) ; blob-index.jsonl = registre des URLs ; cron chaîné sync.py && blob-push.py toutes les 30 min ; backfill 58/58 fichiers 5,3 Go vérifié (ftyp + content-length). Recherche : `/root/rush-archive/find.sh <nom|assetId|user_xxx|projectId>` ; doc : /root/rush-archive/README.md. Rétention backups Convex portée à 30 j (expirationDeltaSecs 2592000, confirmé). RIEN dans l'archive ne se purge jamais. RECHERCHE OCTETS ÉPUISÉE pour l'incident : cache Chrome Jonathan fouillé (156 URLs uuid extraites, 37 mortes toutes en 404 : le CDN ne garde rien), worker rush-proxy VPS nettoie derrière lui (in/out.mp4 supprimés), streaming export n'expose PAS _storage. Dernier porteur possible des 61 fichiers : les appareils et le cache navigateur de Rafaela.

**ARCHITECTURE FINALE RETENUE (Jonathan 25/08 23h, REMPLACE le miroir VPS/Blob comme mécanisme principal)** : tout reste DANS Brvndlab/Convex. Table `rush_archive` (schema) + `hardDeleteOneAsset` n'appelle PLUS JAMAIS `storage.delete` : une suppression définitive (purge corbeille 30 j, nettoyages) déplace la fiche complète (commentaires embarqués, storageIds intacts) dans rush_archive avec raison ; les BLOBS SONT ÉTERNELS dès l'upload (aucun cron nécessaire). Sous-titres remplacés/retirés archivés aussi. Porte admin (internes) : `rushArchive:listArchivedFor {clerkUserId}` (avec fichierVivant) et `rushArchive:restoreFromArchive {archiveId}` (fiche revient visible, restoredAt posé). PROUVÉ en prod le 25/08 : cycle créer→supprimer→archivé fichier vivant→restituer→resupprimer→réarchivé, zéro octet perdu (commit bd8cdb5e). Le cron VPS et la poussée Vercel Blob sont DÉSACTIVÉS (décision Jonathan « pas d'outils tiers ») ; /root/rush-archive (5,3 Go) et le store Blob rush-archive-legal restent en copie froide du 25/08, ne pas purger sans son accord. Backups Convex quotidiens 30 j + corbeille 30 j toujours actifs. ATTENTION dérive de schéma inter-instances : champ `inspirations.source` reposé large (comme `slides`).

**CHANTIER PARCOURS LEAD NIVEAU TRAKYO (nuit 25→26/08, commit b07d2fa2, déployé + vérifié live)** : déclencheur = « clic sur smartlink ça n'a aucun sens ». Audit : placement (trackingLinks.position bio/description/story/dm/ad/post), referrer (touchpoint.source), user-agent (touchpoint.metadata), ville/pays existaient déjà mais n'étaient JAMAIS affichés. Livré : (1) getVisitorJourney joint le lien → linkSlug/placement/linkLabel/sourceVideoUrl/destinationLabel ; (2) timeline Leads : « Clic — lien en bio/description/… » + sous-ligne contenu/« Vers : destination » + chips réseau/appareil (dérivé UA)/ville + type visit « Page visitée » ; purge du code mort comment/dm/sentiment (déclarés côté front, impossibles côté schéma) ; (3) pages vues : schema type "visit"+pageUrl, mutation tracking.recordVisit (visiteur connu seulement, dédup 30 min), route /api/track/visit (miroir de /engage), beacon pageVisit() dans brvnd.js (boot + reset SPA, dédup locale 30 min) — chaîne testée live (clickId inconnu → visiteur_inconnu, zéro écriture) ; (4) tracking.contentAttribution : CA premier ET dernier contact par lien (parcours par visiteur, dernier clic avant vente), taux avec lowSample (<5), rôle decouverte/closeuse/equilibre + tableau « Attribution par contenu » dans Smart Links (CSS .attrib dans slk.tsx). Vérifié robot sur lead réel : « Clic — lien en bio · Vers : Chaine Youtube perso · YOUTUBE MOBILE PARIS, FR ». Référence produit : Trakyo (trakyo.io) — doc API publique llms-full.txt + api.trakyo.io/openapi.json ; leurs événements = click/visit/form_submission/booking/stripe_payment avec referrer+domain+content ; attribution first/last + Linear (app only) ; un lien par vidéo auto. RESTE (idées) : nommer les lignes d'attribution par titre de vidéo source (aujourd'hui slug si pas de libellé), $/vue en joignant contentMetrics, attribution linéaire, leaderboard contenu enrichi. ATTENTION : Vercel n'a PAS auto-buildé le push b07d2fa2 (webhook muet) → commit vide pour relancer. L'instance Performances travaillait la même nuit sur tracking.ts/leadActions.ts (iClosed) : pulls-rebase fréquents obligatoires.

**FUNNEL DE DÉMO E2E (26/08 matin, demande vocale)** : 2 destinations créées via l'UI robot (Page VividFlow → vividflow.co ; Instagram Brvndlab) + 3 Smart Links (358c7752 Instagram/bio→VividFlow ; d342cdbb YouTube/post→Instagram ; 42a55a0b YouTube/bio→chaîne YT) + le lien iClosed existant 91d294ca. Visiteur simulé iPhone (referers l.instagram.com/youtube.com, pauses humaines) : les 4 cookies différents ont été RECOLLÉS server-side par l'empreinte (3 fiches alias mergedInto) → UN lead « Julien Vernet » (julien.vernet.demo@gmail.com, +41 79 123 45 67), intent burning, RDV RÉEL réservé jeu 27/08 10h00 Google Meet (à annuler manuellement). Fiche live : 4 clics avec placement+libellé+slug+YOUTUBE/INSTAGRAM+MOBILE+ville, puis Appel réservé ICLOSED. OBSERVATION : 2 touchpoints booking (10:07 « Appel de Kick off » né de la complétion du FORMULAIRE d'un essai précédent, 10:11 « STRATEGY_CALL » de la vraie résa) → le webhook iClosed enregistre la complétion de formulaire comme booking ; à requalifier en opt_in un jour. Astuce robot : les scripts .mjs doivent être DANS le repo (ESM résout node_modules par chemin du script), pattern .robot-tmp.mjs supprimé après usage.

**GARANTIES iCLOSED (26/08 midi, commits jusqu'à sync-one)** : (1) résolution « le clic du moment l'emporte » VÉRIFIÉE par 2 tests humains réels (Jonathan 11h29 : fiche du clic nommée thomalves12 en 23 s clic→booking ; Rafaela 11h33 : rafaela@costafra.com en <50 s) ; (2) REPRISE ÉCLAIR : recordClick vers *iclosed.io* arme ctx.scheduler +20/45/90/180/480 s → internal.iclosedSync.repriseEclair → POST /api/integrations/iclosed/sync-one {ownerClerkUserId} (secret x-brvnd-secret) → synchroniserIclosed du compte ; garantit ≤30-45 s pour TOUT compte SANS webhook (l'API iClosed ne crée pas de webhooks ; cron global reste 15 min) ; idempotent par externalEventId ; (3) formulaire complété sans hookType booking → kind:"optin" = « Email capturé » (suffixe _optin sur l'id) ; (4) colonne Rendez-vous : repli sur createdAt futur du booking (creneauDe + nextBookingAt) ; (5) Calendly/Stripe/Whop : visitorIdHint honoré partout via findVisitorForOwner (suit les replis), jetons par événement (pas de mémoire sticky come iClosed). PIÈGE ROBOT : Playwright headless = UA HeadlessChrome → filtré comme BOT par le redirect (aucun clic écrit) → un test E2E robot du parcours clic→booking finit en fiche orpheline par email ; tester en humain ou changer l'UA. RDV de test à annuler : Julien Vernet jeu 27/08 10:00, Camille Perrin jeu 27/08 16:00 (+ bookings thomalves12 09:00/11:00). Signature Svix iClosed en mode observation (ICLOSED_SIGNATURE_ENFORCE non posé).

**REFONTE SCORE LEADS + ÉPURE (26/08 midi, commit ceb3a461)** : plancher DE SCORE par étape dans computeHeatScore (e-mail laissé=40 min, RDV récent <60j=65 min, show-up/gagné=85 min, vente=100 verrou) — l'ancien plancher ne relevait que l'ÉTIQUETTE d'où « 30 · CHAUD » ; désormais intent=scoreToIntent(score floored), contributeur visible « Étape : … ». Recalc global exécuté (13+27 fiches, heatScore:recalculateAllForUserFromServer avec secret via `npx convex env get AI_USAGE_SECRET --prod` — vercel env pull rend les secrets VIDES, ne pas s'y fier). Guide « Comment ça marche » : 3 colonnes (étape/score/signification), recommandations RETIRÉES (décision Jonathan : trop prescriptif). « Attribution par contenu » RETIRÉE de Smart Links (décision Jonathan : les leads vivent dans Leads) — query tracking.contentAttribution DORMANTE réactivable. Vérifié live : Camille 65 CHAUD, Thomas 65 CHAUD + rendez-vous, guide 3 colonnes, attribution absente. NOTE récurrente : le webhook Vercel rate parfois un push → commit vide pour relancer.

**LOCALISATION PRÉCISE DES CLICS (27/08, commit apres ceb3a461)** : PLAFOND PROUVÉ de la géoloc IP — sur l'IP Sunrise de Jonathan (Suisse romande), Vercel + ip-api.com + ipwho.is répondent TOUS « Zurich » ; reverse DNS `cgn.dynamic.upc.ch` = Carrier-Grade NAT déclaré au siège (Glattpark ZH). Aucun fournisseur ne peut faire mieux, la donnée source n'existe pas. Sur iPhone s'ajoute le RELAIS PRIVÉ iCLOUD : sortie Apple pour la Suisse souvent à Paris (100% des clics « Paris » en base étaient des iPhone, 100% des « Zurich » des desktops ; liste officielle mask-api.icloud.com/egress-ip-ranges.csv). SOLUTION EN 3 COUCHES : (1) src/lib/tracking/geoQuality.ts détecte les sorties relais (préfixes 172.224-227., 104.28.) → ville/région ÉCARTÉES, pays gardé, champ `geoSource` ("precise"|"reseau"|"relais") sur trackingTouchpoints ; (2) /api/track/geo + geoPrecise() dans brvnd.js : position du NAVIGATEUR (permission `granted` SEULEMENT, jamais de popup — navigator.permissions.query, 1× par mois par appareil) → géocodage inverse Nominatim zoom 14, priorité village>hameau>ville (Grandvaux donnait « Lutry » en zoom 12) ; coordonnées JAMAIS stockées, seulement ville/région/pays ; mutation tracking.recordPreciseLocation corrige la fiche ET ses 50 derniers clics ; (3) affichage Leads : ville exacte sans marque, « ~ ville » si réseau, « FR · via relais privé » si relais. Validé : Lausanne/Sion/Genève/Zurich/Paris exacts. Rattrapage `leadRepair.marquerClicsRelais` exécuté (4+2 clics). Vérifié live sur la fiche Rafaela : plus de « Paris ».

**Process exigé par Jonathan** : chef de projet, to-do détaillée, agents experts + mini-tribunal sur chaque problème, recherches externes si besoin, GARANTIES, et dossier Vercel avant/après (« VSL ») = mettre à jour https://brvndlab-mockups.vercel.app/brvndlab-dossier-publier ou page dédiée brvndlab-dossier-rush-pipeline. Vérif live : robot Playwright + sign-in token Clerk (user_3CaB4NBRnQBy0vDGkydiSNzWdP3), lecture seule.

Voir [[project-brvndlab-chantier-publier-2026-08-24]] pour les précédents (deploy rail, ONBOARDING file à ne jamais committer, /reseaux renommé /performances par Thomas).
