---
name: brvndlab-linkedin-abonnes-extension-source
description: Abonnés LinkedIn (profil perso) — seule source réelle = extension navigateur → accountMetricsDaily.linkedinFollowers ; ne JAMAIS écrire des relations DMA dans followersCount.
metadata: 
  node_type: memory
  type: project
  originSessionId: 100d0ff3-e583-486e-ade2-ef55a5f52457
  modified: 2026-08-18T06:56:23.082Z
---

Résolu le 2026-08-17 (« audience 343 au lieu de 710 »). Hiérarchie des sources d'abonnés LinkedIn pour un PROFIL PERSONNEL (LinkedIn ne les expose ni par API ni par UI) :
1. **Extension navigateur** : lit les abonnés sur la page profil, dépose dans `accountMetricsDaily.linkedinFollowers` (`extension._poserAbonnesLinkedin`). C'est LA vérité (377 pour Jonathan).
2. Zernio : abonnés uniquement pour une PAGE ENTREPRISE ; profil perso → seulement des relations (`linkedinConnections`, hors total).
3. Page publique scrapée (`socialConnections.followersCount`) : ne marche jamais côté serveur (mur 999/authwall).

**Bug corrigé (commit e7cec95-ish, « relations DMA ne se font plus passer pour des abonnés »)** : le cron 05h05 (`refreshAllLinkedInAudience`) écrivait en repli les relations du Member Snapshot DMA — archive PARTIELLE (10 renvoyées pour 377 réelles) — dans `followersCount`, et la préséance socialConnections-d'abord écrasait le 377 de l'extension. Corrections :
- `refreshFollowersFor` : plus aucune écriture DMA ; rend le dernier relevé réel connu. `dmaConnectionsCount` supprimée.
- `snapshotAllAccountsDaily` + `mergeZernioAccountSnapshot` : REPORTENT le dernier `linkedinFollowers` connu (sinon chaque matin/heure repartait à vide et écrasait le dépôt extension).
- `social._clearFollowers` : purge d'un compteur contaminé (utilisée une fois sur la ligne linkedin-dma de Jonathan).

Cadence réelle assumée : IG/YT via Zernio horaire (cron :50) + sync au chargement de l'accueil ; abonnés LinkedIn rafraîchis quand Jonathan visite son profil LinkedIn avec l'extension, reportés entre-temps. Voir [[brvndlab-social-connection-single-truth]].

**⚠️ App self-serve DÉSACTIVÉE par LinkedIn (17/08)** : l'app LinkedIn `788740yvziufcj` (intégration Nango `linkedin-dma`, scope r_dma_portability_self_serve, créée le 04/07) répond « The application is disabled » sur sa page OAuth. L'app `784asjtz8s4rnk` (`linkedin-3p`, scope r_dma_portability_3rd_party) est VIVANTE et donne le même Member Snapshot ; `readConnection` la préfère déjà. Le bouton « Autoriser mon historique » ouvre désormais `linkedin-3p`. Si quelqu'un veut réactiver la self-serve : portail développeur LinkedIn, vérifier qui possède l'app et la raison de la désactivation.

**🔑 DÉCOUVERTE MAJEURE (17/08 soir) — commentaires historiques RÉCUPÉRABLES** : la page publique d'un post (`linkedin.com/feed/update/<urn>`) servie avec UA **Googlebot** répond 200 avec un **ld+json** complet : `comment[]` = {author.name, text, datePublished, likes}, commentCount, articleBody. C'est la SEULE voie serveur pour l'historique des commentaires (UA navigateur → mur 999). `social.recupererCommentairesPublics` la lit (600 ms entre posts) → `linkedinCommentaires` ; 125 commentaires récupérés sur les 27 posts de Jonathan. Le changelog complète en continu pour l'avenir. Affichés dans la modale `.apwin` (fil sous les cartes Likes/Commentaires à icônes).

**Carrousels — matrice finale** : multi-images → détection embed (list-item>1, occurrences PAS lignes) ✓ ; documents PDF HISTORIQUES → invisibles dans les 8 sources testées (archive MEMBER_SHARE_INFO MediaUrl vide, RICH_MEDIA ne liste que les « feed photos », embed muet, page publique robot + ld+json sans trace, ugcPost embed 404, API socialActions 403, fournisseur ignore les posts non publiés par lui, changelog >28 j expiré) → seule issue : marquage manuel par Jonathan (mutation directe) ; documents FUTURS → couverts (posts Brvndlab typés à la source, posts natifs via shareMediaCategory du changelog).

**Aperçu = NOTRE design + commentaires (17/08, 4e vague, remplace la modale embed)** : Jonathan a rejeté l'iframe embed (branding LinkedIn + faux boutons). La modale `.apwin` (Metrics.tsx) est un rendu Brvndlab : gauche = auteur/date + visuel ou carrousel navigable + texte complet ; droite = Likes/Commentaires + fil des commentaires + « Voir sur LinkedIn ». Données via `social.publicationDetail({url})` (match par identifiant numérique d'URN). **Commentaires (texte)** : SEULE source = Member Changelog (événements CREATE, table `linkedinCommentaires`, capturés au fil de l'eau par le cron horaire) — routes socialActions 403 avec nos scopes, archive DMA = données du membre uniquement, fournisseur marque blanche ignore les posts publiés hors de lui (vérifié le 17/08) → l'historique des commentaires d'avant l'autorisation est IRRÉCUPÉRABLE, l'assumer. **Carrousels** : 2 formes — multi-images (embed `feed-images-content__list-item` >1 → media "carousel" + toutes les images) et documents PDF (invisibles dans l'embed ; futurs posts natifs typés via `shareMediaCategory` du changelog, posts Brvndlab typés à la source ; l'HISTORIQUE des documents reste « texte », aucune source ne l'expose). **Textes archive** : `nettoyerTexteArchive` (guillemets CSV « "" ») appliqué à l'import + `_reparerTexteImportes` (réparation idempotente).

**⚠️ PAGINATION ARCHIVE = PAR BLOC (18/08, vérifié 2 comptes prod)** : `memberSnapshotData` → `start` = numéro de BLOC, `paging.total` = nombre de BLOCS (pas de posts). Un bloc peut contenir 27 posts. L'ancien code faisait `start += rows.length` → sautait au bloc 27 inexistant → un client à plusieurs blocs perdait tout sauf le premier. Correct : `start += 1`. Plafonds portés à 300 (historyLive, `_importesTous`) ; lots 80 (enrichissement) / 60 (classement) avec auto-relance. Format « texte » posé explicitement quand l'embed ne montre AUCUN média — sinon le cron de rattrapage boucle à vide sur les posts texte (constaté : 10 posts relancés en boucle).

**GARANTIE NOUVEAUX CLIENTS (audit 18/08)** — 3 trous fermés pour que personne ne revive les cartes vides : (1) import à 0 nouveauté (reconnexion, archive déjà à jour) planifie QUAND MÊME enrichissement + classement ; (2) `enrichirMediasImportes` et `classerPostsLinkedIn` se relancent tant qu'il reste du travail (lots de 80/60 — un client a déjà 68 posts, il aurait été tronqué) ; (3) cron quotidien `social.rattraperEnrichissements` (04h40 UTC) rattrape tout compte incomplet, quelle qu'en soit la cause — testé en prod : a détecté et relancé un compte client réellement incomplet. RÈGLE : toute nouvelle donnée dérivée d'un post (format, thème, likes…) doit être posée par la chaîne d'IMPORT, pas seulement par le composer, ET avoir son rattrapage dans ce cron.

**Classement thèmes/types (18/08)** : cartes « Principaux thèmes »/« Types de contenus » lisent `category`/`postType` du JSON — remplis uniquement par le composer. `social.classerPostsLinkedIn` (Haiku, 1 appel batch, jamais par-dessus un choix utilisateur) classe les importés ; planifié +30 s après chaque import. Thèmes = les 5 du composer LinkedIn ; types = les 4 axes du radar. Récolte connectée 17/08 : 222 commentaires complets (avatars/titres/deMoi dans linkedinCommentaires, images rapatriées `ctx.storage`) + pages carrousels (3/5/5) via pont clipboard Playwright→pbpaste. `ingererRecoltePublication` = l'ingesteur.

**🔑 CARROUSELS-DOCUMENTS RÉSOLUS (17/08 soir, vérifié en session LinkedIn connectée de Jonathan)** : un post document/carrousel PDF porte un urn **`ugcPost`** (jamais `share`), son embed public répond **404**, et ses pages sont des images `ads-document-images_N` (N = numéro de page) visibles UNIQUEMENT connecté. Règle codée dans `enrichirMediasImportes` : ugcPost + embed 404 → media "carousel". Les 3 carrousels de Jonathan marqués avec page 1 en vignette (URLs licdn à expiration ~10 j — l'extension/un futur passage connecté devra les rafraîchir). Répartition réelle de ses 27 posts : 10 textes / 14 images (1 seule image chacun, vérifié) / 3 carrousels / 0 vidéo. « Faites ce test » = TEXTE (pas un document, contrairement à l'hypothèse du matin). Pour naviguer LinkedIn connecté : Jonathan se logue dans la fenêtre Playwright (comme pour Brvndlab), puis sweep des URLs /feed/update/<urn>/ avec extraction des `dms/image|document|playlist` hors profile/company-logo.

**Formats + aperçu (17/08, 3e vague)** : l'archive DMA ne dit JAMAIS le format d'un post (MediaUrl quasi toujours vide) → `social.enrichirMediasImportes` lit la page embed publique après chaque import (marqueur `feed-images-content` → photo + première image `data-delayed-url` hors `profile-displayphoto` en vignette ; `<video>` → video). Limite assumée : un carrousel-DOCUMENT n'expose rien dans l'embed → reste « texte ». Vérifié sur les 27 posts de Jonathan : 14 photos détectées (48/52 dans Formats de posts). Clic sur une ligne publiée du tableau Metrics → modale `.liwin` avec l'embed officiel LinkedIn (`/embed/feed/update/<urn>`, 504px natif) sur fond flouté `.lbox` ; les commentaires ne sont pas embarquables (bouton « Ouvrir sur LinkedIn »). Brouillons → éditeur comme avant.

**RÈGLES SCELLÉES 17/08 (2e vague)** :
1. Les publications de réseau (`kind:"social_post"`, importées OU parties de l'app) ne passent JAMAIS par le cron d'archivage du pipeline (`archiveOldPublished` les skippe) — ce cron avait archivé les 27 posts importés en 1h (« mes posts ont disparu »). `contentItems._restoreSocialPosts` répare si jamais ça revient.
2. Compteur fournisseur LinkedIn = ABONNÉS même pour un profil personnel (vérifié : profil réel 378 abonnés/189 relations → fournisseur rend 378). `zernioData.readLinkedin` mappe compteur→followers ; `getMyLinkedInStats.connections` = null tant qu'aucune source ne mesure de vraies relations ; carte Relations masquée (fin du doublon 378/378). La CARTE ACCUEIL compte les ABONNÉS.
3. TOUTE troncature de texte LinkedIn passe par `tronqueNet`/`bienForme` (exportées de social.ts) — 3 crashs distincts par émoji/gras Unicode coupé en unités UTF-16 (import, mutation, `_importesLinkedin` de historyLive).
4. États vides des Metrics = le visuel NU (courbe, damier, cadran, radar, lignes « — ») — jamais de texte-consigne (demande Jonathan). Note « cumul depuis le début » DANS la colonne kcol, jamais entre les cellules de la grille krow (elle éjectait le graphe Performances).

**Import d'archive RÉUSSI (17/08, 27 posts)** : deux pièges d'affilée dans le parsing — l'export LinkedIn tronqué en plein émoji + nos `slice(0,90)` qui fendaient un surrogate pair → Convex refusait l'écriture (« unexpected end of hex escape »). Corrigé par `bienForme()`/`tronqueNet()` dans social.ts (assainir TOUT texte venant de l'archive, tronquer par codepoints, jamais par unités UTF-16). Le tableau Analytics LinkedIn (Metrics.tsx) fusionne désormais l'historique live Zernio + les posts importés (dédup URL puis minute) — avant, il ne montrait que le live et le tableau restait vide. Période par défaut : 12 derniers mois (demande Jonathan). L'archive ne donne JAMAIS les likes des posts importés (limite LinkedIn), le live Zernio les complète quand il les connaît.

**Historique LinkedIn (cycle rejouable, 17/08)** : l'import des posts passés passe UNIQUEMENT par l'autorisation de portabilité DMA (connexion Nango `linkedin-3p` désormais, Member Snapshot MEMBER_SHARE_INFO, version 202312). Elle SAUTE à chaque déconnexion (voulu : token + connexions purgés). Design validé Jonathan : (1) modale « dernière étape : ton historique » à CHAQUE connexion LinkedIn réussie (marque blanche ET Nango), (2) bannière persistante sur Intégrations tant que LinkedIn est branché sans l'autorisation (« Plus tard » = masquée pour la visite seulement), (3) au retour d'autorisation, sync + import auto ; archive pas prête → cron autoImportAll (30 min) la récupère. La nouvelle connexion DMA traverse la pierre tombale car plus récente. Zernio ne fournit JAMAIS le contenu des posts passés, seulement stats/métriques.
