---
name: project_brvndlab_reglages_admin_conso_sealed
description: "Brvndlab — Réglages, Intégrations, Consommation client, Admin fondateur, favicon/email/légal : validés et scellés (MVP brique 1)"
metadata:
  node_type: memory
  type: project
  originSessionId: 03adbfc3-e0b4-4f12-87ce-d3de3ed326f9
---

Chantier Réglages + Admin validé et scellé dans le moodboard (`brvndlab-moodboard.vercel.app`). Cadre clé : **DÉCOUPAGE EN 2 BRIQUES**. Brique 1 = MVP actuel = moteur de contenu (Brand OS → Brainstorm → Génération → Pipeline → Calendrier → Journal → Publication) + Réglages/Intégrations/Consommation/Admin/Auth/Pricing/Légal. **Brique 2 = PAS dans le MVP = leads + PAIEMENT + tracking + ventes** (Smart Links, Offres, Bibliothèque de liens, statut paiement, Stripe/Calendly/iClosed). Ne rien afficher de la brique 2 maintenant (sinon « faux-vide »).

**Réglages** (`reglages-desktop.html`, cartes empilées) : Profil (photo, nom, email + **corps de métier**, **entreprise** optionnel, **handle**) ; **Utilisation** (carte conso) ; Notifications (1 toggle push) ; **Intégrations** (ligne) ; Sécurité (Déconnexion seul, détail dans une **modale après clic**). Retirés : la notif « notes de ton coach » (vestige coaching), Apparence (doublon du toggle thème du header), Bibliothèque de liens + Offres (brique 2), tous les sous-textes superflus. Handle = `businessHandle` du backend (identifiant unique Brvndlab, pas par réseau).

**Intégrations** (`integrations-desktop.html`) : « Connexions » renommé **Intégrations** (naming gelé, « Smart Links » réservé brique 2). Section **Publication** = 4 réseaux connectables : **Instagram, Facebook (Meta) [requis pour publier IG], LinkedIn, YouTube**. Exclus (brique 2) : Stripe/Calendly/iClosed/Whop/GHL/Skool. Zéro TikTok ([[feedback_no_tiktok]]).

**Consommation client = « Utilisation »** (`utilisation-desktop.html` + carte Réglages) : façon ChatGPT/Perplexity. Montre **générations X / 100 aujourd'hui** + barre, tendance 7 jours, répartition par usage (Brainstorm/Scripts/Carrousels/Stories/Journal). Unité = « générations ». **RÈGLE ABSOLUE : jamais exposer le modèle/provider/coût €/tokens** côté client (backend `aiUsage` : limite `DAILY_USER_LIMIT=100` appels/jour fixe, non liée au plan). Endpoint client = payload neutre {count, limit, remaining, parModule, serie7j}.

**Admin fondateur** (sidebar « Admin », founder-only) : `admin-clients-desktop.html` (compteurs Total/Actifs 7j/Inactifs, table Client·Plan·Accès·**Activité (Actif/Inactif)**·Actions, œil = **Aperçu client** = vue Utilisation telle que le client la voit) + `admin-couts-desktop.html` (vue précise founder : coût €, appels, tokens, **modèle visible** Sonnet/Haiku, par feature, par modèle, derniers appels, sélecteur de période). Statut **paiement** (à jour/en retard) remplacé par **Activité** en MVP (Stripe = brique 2). Gate = allowlist email fondateur.

**Détails** : favicon officiel `/favicon.ico` (mark hexagonal orange) injecté sur tous les écrans + index. **Email de confirmation** redessiné à la DA (`email-confirmation.html`) : logo officiel `/logo.png`, palette warm crème, code de vérification, fini l'orange/#EEF0F4 (le vrai template Clerk + `convex/emails.ts` lien `#f97316` restent à corriger côté repo par le CTO). **Pages légales** (`legal-desktop.html`) : Conditions / Confidentialité / Mentions, DA, éditeur VividFlow LTD Londres.

**ÉVOLUTION — Réglages premium en HUB + sous-pages** (validé) : `reglages-desktop.html` = un HUB menu (groupes Compte / Application / Légal), chaque domaine ouvre sa propre page focalisée : `profil-reglages.html` (nom + **photo upload fonctionnelle** + corps de métier + entreprise éditables inline), `compte-reglages.html` (Email modifiable via modale code + Mot de passe via modale), `securite-reglages.html` (**Sécurité & sessions** : appareils connectés via Clerk useSessionList + session.revoke + "déconnecter partout", 2FA = "Bientôt"/repoussé, déconnexion), `notifications-reglages.html`, + Utilisation/Intégrations/Légal existants. **Suppression de compte RETIRÉE des Réglages** (Jonathan : pas envie que ce soit à portée de clic ; à remettre plus tard via un parcours dédié).

Spec backend pour le vrai code (tribunal 4 juges) : Convex = ajouter `jobTitle` (v.optional) + brancher jobTitle & businessName dans `updateProfile`. Bug photo = `handlePhotoUpload` échoue en silence -> ajouter res.ok + try/catch + toast + validation type/taille. Clerk ^7 couvre email (createEmailAddress+verification), mot de passe (updatePassword), sessions (useSessionList + session.revoke), 2FA (createTOTP), delete (user.delete). Retirer 3 vestiges coach (badge "Rôle coach", notif "notes de ton coach").

**BLOCAGE PROD à lever avant de pousser dans le vrai Brvndlab** : la prod est déployée manuellement (`vercel --prod` depuis un checkout local, pas d'auto-deploy git), et la branche live n'est pas confirmable de façon fiable (main = 1er juin trop vieux ; `claude/brandos-server-enforcement` = 26 juin avec du WIP non lié ; `recover/pipeline-darkmode-745bcd8` = 22 juin). Un `vercel --prod` embarque toute la branche -> NE PAS pousser au pif. Demander à Jonathan/CTO depuis quelle branche on déploie, puis preview-first ([[feedback_brvndlab_preview_first_mode_loupe]]) avant prod.

**LOGIN/SIGNUP IMAGES — SHIPPÉ EN PROD (2026-07-01)** : les gravures sont EN LIGNE sur app.brvndlab.com/sign-in ET /sign-up. Le vrai login/signup est une page custom (getClerk singleton, PAS les hooks React à cause d'un conflit @clerk/clerk-react via Convex) qui utilise le composant `src/components/AuthShell.tsx` (split : formulaire gauche + card image droite). J'ai patché la card droite de `AuthShell.tsx` pour afficher `/login-dark.png` (thème sombre) ou `/login-light.png` (clair) selon l'état `mode`, retiré l'import BrvndMark devenu inutile, ajouté les 2 images dans `public/`. Déployé depuis la branche de release **`recover/pipeline-darkmode-745bcd8`** (commit "release: recover/ = dernière release" = ce qui tourne en prod), commit poussé sur `origin/loupe-login-images` (à merger dans le flux release). Déploiement : `vercel --prod --yes --scope jonathanzekhe-4288s-projects` depuis `~/dev/brvndlab-claude/brvndlab-app` (build frais = clé Clerk NEXT_PUBLIC présente ; ⚠️ les previews Vercel n'ont PAS la clé Clerk -> 500 en preview, normal). PNG ~3 Mo à compresser en WebP. Login+signup partagent AuthShell -> une modif règle les deux.

**LOGIN /sign-in (note historique)** : le design scellé (split formulaire + illustration à droite, clair/sombre) est demandé sur la vraie page. Images fournies par Jonathan dans `Documents/Claude AI/Brvndlab/brvndlab-app/Visual asset/` : `Log-in-claire.png` (clair) + `Log-in-fonce.png.png` (sombre, double extension). Slot image = **546 × 589 px** (retina 1092 × 1178), `object-fit:cover;position:absolute;inset:0`. J'ai réécrit `src/app/sign-in/[[...sign-in]]/page.tsx` (custom pixel-perfect + Clerk useSignIn/OAuth), créé `src/app/sso-callback/page.tsx` (callback OAuth manquant), copié les images en `public/login-dark.png` / `public/login-light.png`. Typecheck OK, NON commité/NON déployé (login = ne pas déployer sans smoke-test connexion, que je ne peux pas faire d'ici). PNG ~3 Mo à compresser en WebP. NB : le sign-in du repo local était encore l'ANCIEN (carte Clerk `<SignIn>` orange `bg-#EEF0F4`) alors que la prod affiche déjà le custom -> le code live du login n'est pas dans ce remote (branche live non identifiable, cf blocage prod ci-dessus).

Voir [[feedback_mockup_visual_only_respect_backend]], [[project_brvndlab_module_separation]], [[feedback_less_is_more_premium]], [[feedback_verify_deploy_lineage_before_prod]], [[project_brvndlab_illustration_style_sealed]].
