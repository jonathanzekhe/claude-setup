---
name: project_vividflow_client_projection_url
description: "Dossiers de projection client VividFlow servis sous vividflow.co/<nom-entreprise>, projet Vercel site-mockups, SSO coupé"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0bc6d1db-ff84-4a76-86ba-7ddcb68f081e
---

Les dossiers de projection personnalisés (signés VividFlow, traités comme une Keynote Apple) sont publiés sous **vividflow.co/<nom-entreprise>**.

- Le projet Vercel **site-mockups** (`prj_r2dBtoTZK6EVvrkBP7XqWg0oJKkL`, team `team_GTs1MknPmT57qmmhxdVCv0sl`) sert aussi le domaine **vividflow.co** + www + site-mockups-omega.vercel.app. Dossier local : `Documents/Claude AI/VividFlow/site-mockups/`. La home reste `index.html`, intacte.
- **Protection SSO désactivée** le 2026-06-06 (ssoProtection était `all_except_custom_domains` → renvoyait 401 sur les URLs `.vercel.app`). Désormais `null` : les déploiements sont publics. Token CLI Vercel : `~/Library/Application Support/com.vercel.cli/auth.json`.
- URL propre via `rewrites` dans `vercel.json` : `{"source":"/schmid-signature","destination":"/schmid-signature.html"}`. Pour un nouveau client : dupliquer le HTML en `<nom>.html` + ajouter un rewrite `/<nom>` → `/<nom>.html`.
- Déploiement : `vercel --prod --yes` depuis le dossier, puis vérifier `vividflow.co/<nom>` en 200 + home toujours 200. Voir [[feedback_always_push_to_vercel]] et [[feedback_always_link_preview_url]].

Premier client : **schmid-signature** (immobilier Valais, gérance/mise en valeur/estimation). Voir [[project_schmid_vividflow_projection_deck]].

DA commune scellée : fond #FAF9F6, SF Pro (`-apple-system`), accent orange #E26137 rare et maîtrisé (un seul fragment/élément par page), zéro carte/colonne, sobriété maximale. Une page = une URL propre, sans barre de contrôle (la barre/pills sert uniquement aux maquettes de review). Animation d'entrée discrète, zéro gadget.

**RÈGLE BRANDING (Jonathan, important) : « Présenté par VividFlow » reste tout en bas de CHAQUE page présentée**, centré, discret (point orange + texte). Ne jamais l'omettre.

Pages scellées :
- P1 `/schmid-signature` — couverture Daft Punk : nom géant fit-to-width JS (réutilisable par client), « Une première lecture de votre activité » (accent orange sur « votre activité ») + bouton « Lancer la projection » qui finit la ligne (geste Aura, réf Aura/Onda).
- P2 `/schmid-signature-2` — compréhension métier : une seule phrase miroir, poids 300, quasi-noir, accent orange sur « valeur d'un patrimoine ». Pas de bouton.
- P3 `/schmid-signature-3` (VALIDÉE 2026-06-06, version finale) — miroir valeur visible vs charge équipe, **pur typographique + mouvement, zéro illustration**. Gauche « CE QUE VOS CLIENTS VOIENT » : 3 résultats poids 300 avec coche verte « validé » (tuile œil verte). Droite « CE QUE CELA DEMANDE À VOTRE ÉQUIPE » : flux mono de **10 tâches** qui scrolle en boucle infinie (`translateY(-50%)`, liste dupliquée, masque fade haut/bas), chaque ligne avec icône **horloge orange** (le temps que ça prend), texte chaud `--warm:#5C5149` (pas gris). Vert validé `#5E7A52` / orange `#E26137`, esprit cartes R2 AVANT/APRÈS. Calme immobile à gauche vs flux sans fin à droite = l'écart.
- P4 `/schmid-signature-4` (VALIDÉE 2026-06-07) — transition « le temps perdu ». Composition centrée : phrase posée (poids 300, interligne aéré 1.58) « Entre les outils métiers, les communications et les décisions, trop de temps se perd dans des `micro-actions` (orange) qui empêchent l'équipe de se concentrer sur ce qui crée vraiment de la valeur. » + 3 chips sources **Outils métiers / Communications / Décisions** (pictos grille/bulle/chevron) + **barre de temps** horizontale (22 segments orange « Temps absorbé par les micro-actions » + 1 filet vert flex 2.4 « Temps consacré à la valeur », segments en entrée staggered). Prépare P5 « c'est là que beaucoup de gérances immobilières plafonnent ». Variante verticale + alignée-gauche écartées. Workflow 3 mockups via `?v=` sur `/schmid-4`.
- P5 `/schmid-signature-5` (VALIDÉE 2026-06-07, version finale) — point d'arrêt business « C'est là que beaucoup de gérances immobilières `plafonnent` (orange). ». **PATTERN VALIDÉ « capacité interne unique » (à garder pour la suite)** : une SEULE barre horizontale (même boîte / même équipe / même capacité) = 6 cartes rouges majoritaires (icône + micro-label : Emails, Relances, Rappels, Suivis, Documents, Coordination) + **1 carte verte minoritaire INTÉGRÉE au bout** (icône courbe ascendante), même écart partout (gap 2px), AUCUN vide entre rouge et vert. Idée à faire passer en 3s : la capacité interne est saturée par les tâches chronophages, il reste peu de place pour la croissance → ça plafonne. Croissance comprimée DANS le même ensemble, jamais isolée/séparée (Hermès a explicitement corrigé : pas deux systèmes, pas de gros vide, pas de flèche/schéma dramatique). 2 labels mono en haut, alignés aux 2 extrémités de la barre : « Tâches chronophages » (gauche, orange) / « Capacité de croissance » (droite, vert). Fills transparents (orange .10 / green .12), sobre/premium. Garder ce pattern pour montrer un déséquilibre opérationnel/valeur.
- P6 bascule `/schmid-6` (review, PAS scellée, en attente validation) — page de transition « À ce stade, la question n'est plus de faire plus. C'est de faire `circuler` (orange) le travail autrement. » : à gauche amas orange (pile), centre la phrase pivot, droite les mêmes items réorganisés en flux gris nommé, fines trajectoires pointillées animées (amas→flux). L'URL `-6` lui est réservée si validée.
- P7 RÉVÉLATION `/schmid-signature-7` (VALIDÉE + scellée 2026-06-07, page finale du deck) — titre « Voilà la `solution` (orange). » + **fenêtre desktop premium** (3 points mac, avatar agent VividFlow `persona/agent-outbound.png` au bonnet/lunettes sur cercle beige, point « actif » orange) avec **conversation animée Marine ↔ bras droit IA** (messages progressifs → typing dots → réponses, rythme lent ~12s ; copy figée fournie par Jonathan). Hiérarchie couleur validée : **bulles IA anthracite #211D19 texte clair (autorité)**, **bulles Marine gris chaud #E8E2D7 (humain)**, **orange = micro-signal seulement** (avatar/point actif/icône fichier). Fin : pièce jointe métier compacte (style fichier iMessage, PAS de carte SaaS) « Demande locataire — suivi · Document · 2 pages » (jamais de nom marketing : pas « dossier préparé/projection/VividFlow »). Puis bouton **« Continuer → »** noir, hors fenêtre, bas-droite, qui apparaît SEULEMENT après la fin (`window.deckRevealNext()` / délai). Avatars de tes agents = `persona/*.webp` (les images collées en chat ne sont PAS des fichiers : Jonathan doit les déposer dans `site-mockups/persona/`).
- P8 RÉSULTAT `/schmid-signature-8` (VALIDÉE + scellée 2026-06-07, dernière page du deck) — « Moins de micro-actions à porter. (1re ligne gris ~70%) **Plus de capacité** (semi-bold) pour décider, suivre et faire grandir **Schmid Signature** (noir dense, jamais orange). » Élément central pack vert **CAPACITÉ DE CROISSANCE** (icône courbe). Mécanique B « réallocation » validée (sur 3 testées A conversion / B réallocation / C remplissage, via `?v=` sur `/schmid-8`) : barre 2 zones, l'orange « MICRO-ACTIONS » se condense (passe à « ACTIONS » quand petit, jamais « MICRO »), le vert s'élargit ; label vert révélé après stabilisation. 4 badges déclencheurs (Demande reçue · Contexte retrouvé · Actions coordonnées · Décision à valider) : à chaque badge, pop du badge + pulse du vert + réduction orange/expansion verte. Sous-titre P7 ajouté « Un bras droit IA qui **prépare, coordonne et sécurise** vos suivis métier. » (verbes denses, 2 lignes), point « actif » de l'en-tête P7 retiré. P7 « Voir le résultat → » câblé vers P8 (P7 `window.DECK_NO_ARROW=true` pour éviter le doublon flèche ronde + bouton).
- P8 BOOKING `/schmid-signature-booking` (scellée 2026-06-07, DERNIÈRE page, DECK_INDEX=7) — page de réservation intégrée dans la DA (PAS une page CTA marketing) : en-tête sobre kicker mono « Prochaine étape » + titre léger « Réservez votre `audit IA offert` (orange). », puis widget inline iClosed (`<div class="iclosed-widget" data-url="https://app.iclosed.io/e/vividflow/audit-out">` + script `app.iclosed.io/assets/widget.js`, height 620px) dans un panneau --win cadré, « Présenté par VividFlow » en bas, points de pagination deck.js. Pas de gros texte marketing. La page résultat P7 (`/schmid-signature-8`) a reçu un bouton **« Continuer l'audit »** (pill noir bas-droite, révélé en fin d'anim, `window.DECK_NO_ARROW=true` pour remplacer la flèche auto) qui mène au booking. **Structure finale = 8 pages** : couverture / compréhension / écart / temps perdu / plafond / révélation(-7) / résultat(-8) / booking. Règle confirmée : la dernière étape du dossier = réservation intégrée même univers visuel, jamais une redirection externe brute.
- **NAV DECK changée (deck.js 2026-06-07)** : la flèche « suite » est maintenant un **rond noir + flèche droite** en bas à droite (`.deck-next`), qui **apparaît après que la page est animée** (délai ~1.1s, ou `window.deckRevealNext()` pour les pages à longue anim). Sur toutes les pages sauf la 1ère (qui garde « Lancer la projection ») et sauf la dernière (pas de page suivante). Plus de flèche grise légère. Points de pagination en haut conservés.
  - **LEÇON forte (ne plus y revenir)** : sur ce deck, ce qui marche = typo + mouvement dans la langue des pages, PAS de casting de personnages. ~10 directions illustration rejetées (SVG plat, scènes, iceberg, split-screen perso, flat Canva). L'illustration est écartée. Mon générateur d'images dispo (Canva) ne produit PAS le style anime cel-shadé des personas VividFlow (pixelskyline) ; toute illustration future passe par le pipeline image-to-image de Jonathan, pas par moi.

Navigation deck (`/deck.js`, partagé) : points de pagination en haut (actif orange, cliquables), branding en bas. Page 1 = bouton « Lancer la projection » qui enchaîne. Pages 2+ = **flèche légère** en bas à droite (grise, sans cadre, pas de pill central — Jonathan a rejeté le bouton « Continuer » centré comme trop présent). Flèches clavier ← →. Ajouter une page = 1 ligne dans `PAGES` de deck.js + `window.DECK_INDEX` dans la page + rewrite dans vercel.json.

Workflow Hermès↔Claude Code : Hermès/Jonathan = copy + trame + validations de fond ; Claude Code = exécution visuelle + variantes + intégration. Je propose sur le lien → QA → je corrige → sceau. On verrouille page par page, on ne re-touche pas une page validée.
