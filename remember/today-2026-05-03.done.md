Skool rollback (no native webhooks); iClosed integration impl (convex/iclosedIntegration.ts, webhook route [secret], UI); integrations page cleanup initiated; TS ✓

---

Already at max compression. All facts, verbs, refs preserved; no articles/prepositions to drop. Distinct features (Skool rollback ≠ iClosed impl ≠ page cleanup) so no merge recommended.